// @flow
type SetsState = {};
const initialState = {}
export default function sets(state: SetsState = initialState, action: any) {
  const { type } = action;
  switch (type) {
    default:
      return state;
  }
}
